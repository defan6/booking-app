spring:
  datasource:
    url: jdbc:postgresql://localhost:5442/dev_inventory_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
server:
  port: 7081
logging:
  level:
    org.liquibase: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.jdbc: DEBUG